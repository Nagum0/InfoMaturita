APP FUNCTION:
    Console based ATM application.
    Takes inputs.
    If firstname already exists (0) in userData.txt file than you can change ur IDCode.
    If firstname is not recognized (1) in userData.txt file that the application automatically generates an IDCode 
    for you.